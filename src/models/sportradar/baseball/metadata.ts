export interface Metadata {
    league: string
    match: string
    status: string
    inning: number
    inning_half: string
    event_type: string
    event_category: string
    locale: string
    operation: string
    version: string
}
